# UML-Spezialist02 Commitnachweis

## Umsetzungsplan (abgeleitet)
1. Spielzustand um messbare Zaehler erweitern (Level/Teilaufgaben).
2. Fortschritt als Prozentwert aus Zustand ableiten.
3. `render_game_to_text` als kompakte JSON-Schnittstelle bereitstellen.
4. `advanceTime` als deterministische Test-Schnittstelle bereitstellen.
5. Smoke-Test mit Screenshot + State-Ausgabe als Nachweis.

## Auf diese Spiele angewendet
- `SELECT/column_sieve`
- `INSERT/value_match`
