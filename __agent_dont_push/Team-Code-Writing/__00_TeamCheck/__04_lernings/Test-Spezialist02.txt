Sowohl Screenshot als auch state-0.json sind notwendig: Screenshot validiert UI/Interaktion, state-0.json validiert Messbarkeit fuer den LocalStorage-Fortschritt.

Batch 04: Smoke-Validierung mit state-0.json priorisieren; vorhandene 404-Noise getrennt von Messbarkeitskriterien bewerten.

Batch 05: Bei Hook-Regressionen zuerst Spezialfaelle testen (z. B. State-Machine mit scenarios), weil dort level_total-Fehler sofort sichtbar werden.

Batch 06: Hook-Regressionen auch auf Startzustand pruefen (progress=0), nicht nur auf 'Hook vorhanden'.

Batch 07: Bei Hook-Tests simulated_ms als zusaetzlichen Stabilitaetsindikator pruefen (nicht nur progress_percent).

Batch 08: Wrapper-Smokes gegen game_pages-Engines explizit mit aufnehmen, nicht nur direkte _gjs-Seiten testen.

Batch 09: Bei Serien-Smokes fuer mehrere Spiele 404-Noise gesondert klassifizieren, solange render_game_to_text stabil bleibt.
