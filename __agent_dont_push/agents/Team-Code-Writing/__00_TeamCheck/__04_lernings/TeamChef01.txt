2026-02-17
- Tree-Probleme kamen aus zwei kombinierten Ursachen:
  1) sichtbare Technik-Unterordner (`assets`, `data`, `__dokumentation`) verhinderten Leaf-Icon-Rendering,
  2) `brain`-Fallback überlagerte die gewuenschte Bücher-Logik.
- Stabiler Ansatz: Hidden-Folder in der Normalisierung filtern statt erst beim Rendern.
- Noch robuster: die Sichtbarkeitsregel nur auf `_` aufbauen und Ordner physisch zu `_assets`/`_data` migrieren.
- Wenn ein Knoten per `is-openable` markiert wird, muss `folderEmoji` auch `attributesRelPath` berücksichtigen, sonst entstehen inkonsistente 📁-Icons.
- Korrektur: keine globale physische Migration mehr ohne manuelle Einzelprüfung, da `assets`/`data` in Einzelfällen auch fachliche Ordnernamen sein können.
- Gründliche Hidden-Prüfung sollte immer auf den real sichtbaren Tree (nach Normalisierung) gehen, nicht nur auf Dateisystem-Namenlisten.
- Für bessere UX im Tree: Themenknoten sollten Status-Icons tragen, Spielknoten selbst iconfrei bleiben und per Label-Klick direkt die Spielseite öffnen.
- Spielklick-Logik muss zwischen `generic_page` (Thema) und `generic_c_suite`/direktem Game (Spiel) unterscheiden; bei fehlender HTML-Datei darf ein Spielklick nicht auf `generic_page` zurückfallen.
