@startuml
title Nachhaltigkeitsbenchmark-Monitor - Aktivitaetsdiagramm
start
:Nutzer oeffnet Spielseite (_ghtml_nachhaltigkeitsbenchmark_monitor.html);
:Konfig laden (_data/_gg01_nachhaltigkeitsbenchmark_monitor.json);
while (Runden offen?) is (ja)
  :Szenario lesen;
  :Option waehlen;
  if (Auswahl korrekt?) then (ja)
    :Punkte erhoehen;
  else (nein)
    :Feedback + Abzug;
  endif
endwhile (nein)
:Ergebnis anzeigen;
stop
@enduml
