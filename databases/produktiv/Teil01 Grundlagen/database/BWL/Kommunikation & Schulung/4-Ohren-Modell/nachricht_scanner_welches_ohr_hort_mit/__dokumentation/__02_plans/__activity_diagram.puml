@startuml
title Nachricht-Scanner
start
:Nutzer oeffnet Seite (_ghtml_nachricht_scanner_welches_ohr_hort_mit.html);
:Browser laedt HTML/CSS/JS;
:Game-Script initialisieren (_gjs_nachricht_scanner_welches_ohr_hort_mit.js);
if (Externe Daten verfuegbar?) then (ja)
  :JSON/Leveldaten laden (_data/_gg01_nachricht_scanner_welches_ohr_hort_mit.json);
else (nein)
  :Mit lokalen Initialdaten arbeiten;
endif
:Startzustand der UI rendern;
while (Spielrunde aktiv?) is (ja)
  :Nutzereingabe verarbeiten;
  :Regeln pruefen und Zustand aktualisieren;
  if (Aktion korrekt?) then (ja)
    :Positives Feedback und Fortschritt;
  else (nein)
    :Fehlerfeedback und ggf. Retry;
  endif
  :HUD/DOM neu zeichnen;
endwhile (nein)
:Ergebnis anzeigen;
stop
@enduml
